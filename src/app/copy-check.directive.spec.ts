import { CopyCheckDirective } from './copy-check.directive';

describe('CopyCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyCheckDirective();
    expect(directive).toBeTruthy();
  });
});
